var expect = require('chai').expect;

const Handcrank =
  require("../lib/handcrank");

describe("handcrank", () => {

  it("is contructable", () => {
    const hk = new Handcrank();
    expect(hk).to.be.instanceof(Handcrank);
  });

  describe("data method", () => {

  });

  describe("add method", () => {

  });
})
